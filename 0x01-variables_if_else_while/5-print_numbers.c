#include <stdio.h>

/**
* main - entry point
* Description: this program is Betty compliant
* Return: Success should always be (0)
*/

int main(void)
{
int a;

for (a = 0; a < 10; a++)
{
printf("%d", a);
}
printf('\n');
return (0);
}
